{
  "name": "apex-routing-worker",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check src/index.js && node test.mjs",
    "test": "node test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.114.0"
  }
}
