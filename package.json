{
  "name": "apex-dispatch",
  "version": "3.1.0",
  "private": true,
  "type": "module",
  "description": "Apex Dispatch route and local operating intelligence by BenDESK.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "npm run build && node scripts/test.mjs && npm --prefix worker run check"
  },
  "engines": {
    "node": ">=20"
  }
}
